NAME
    hfi - simple time(1) wrapper

SYNOPSIS
    hfi FILE

DESCRIPTION
    hfi (short for "how fast is") is a wrapper over the time(1) command that shows the time, CPU, and memory use of a Bash script in a pretty format.

EXAMPLE
    hfi test_script
        Assesses a file called test_script
